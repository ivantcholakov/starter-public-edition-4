
{{ css('lib/slick/slick-font.min.css') }}
{{ css('lib/slick/slick.min.css') }}
{{ css('lib/slick/slick-theme.min.css') }}

<style>

/* Slideshow Custom Styles */

.slick-slide {
    outline: none;
}

.slideshow {
    position: relative;
    width: 100%;
    margin: 0;
    padding: 0;
}

.slideshow .carousel {
    width: 100%;
}

.slideshow .carousel:not(.slick-initialized) {
    visibility: hidden;
}

.slideshow .slick-prev {
    left: 15px;
}

.slideshow .slick-prev[dir="rtl"] {
    left: auto;
    right: 15px;
}

.slideshow .slick-next {
    right: 15px;
}

.slideshow .slick-next[dir="rtl"] {
    left: 15px;
    right: auto;
}

.slideshow .slick-prev, .slideshow .slick-next {
    height: 48px;
    width: 48px;
    line-height: 48px;
    font-size: 24px;
    text-align: center;
    border-radius:4px;
    margin: 0;
    padding: 0;
    outline: none;
    border: none;
    box-shadow: none;
    z-index: 1100;
    background-color:rgba(0,0,0,0.3);
    color: #fff;
    transition: 0.3s;
}

.slideshow .slick-prev:hover, .slideshow .slick-prev:focus, .slideshow .slick-next:hover, .slideshow .slick-next:focus {
    outline: none;
    background-color: rgba(0,0,0,0.5);
    color: #fff;
    border-radius: 10px;
}

.slideshow .slick-prev i, .slideshow .slick-next i {
    display: inline;
    margin: 0;
}

.slideshow .slick-prev:before {
    content: "";
}

.slideshow .slick-prev:before[dir="rtl"] {
    content: "";
}

.slideshow .slick-next:before {
    content: "";
}

.slideshow .slick-next:before[dir="rtl"] {
    content: "";
}

.slideshow .slick-dots {
    position: absolute;
    bottom: 10px;
    z-index: 1100;
}

.slideshow .slick-dots li button:before {
    font-family: "Icons";
    font-size: 8px;
    content: "\f111";
    text-shadow:0 0 4px #fff, 0 0 4px #fff, 0 0 4px #fff, 0 0 4px #fff, 0 0 4px #fff;
}

.slideshow .buttons {
    position: absolute;
    bottom: 15px;
    left: 15px;
    z-index: 1100;
}

.slideshow .buttons[dir="rtl"] {
    left: auto;
    right: 15px;
}

.slideshow .buttons button {
    width: 35px;
    height: 35px;
    line-height: 35px;
    font-size: 14px;
    text-align: center;
    margin: 0;
    padding: 0;
    outline: none;
    border: none;
    box-shadow: none;
    z-index: 1100;
    background-color: rgba(0, 0, 0, 0.3);
    color: #ffffff;
    border-radius: 4px;
    transition: 0.3s;
}

.slideshow .buttons button:hover, .slideshow .buttons button:focus {
    outline: none;
    background-color: rgba(0, 0, 0, 0.5);
    color: #fff;
    border-radius: 10px;
}

.slideshow .buttons button i {
    display: inline;
    margin: 0;
}

</style>
